version: "3.3"

services:
  server:
    container_name: spotify-seedem
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/var/www
    ports:
      - "8001:80"
    expose:
      - 80